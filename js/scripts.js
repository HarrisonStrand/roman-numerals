function toRoman(userInputStr){
  let userInput = parseInt(userInputStr);
  userInput;
}


function toRoman(userInputStr){
  let userInput = parseInt(userInputStr);
  userInput;
  if (userInput >3900) {
    alert("No numbers over 3,900!!!")
  }
}


function toRoman(userInputStr){
  let userInput = parseInt(userInputStr);
  if (userInput >3900) {
    alert("No numbers over 3,900!!!")
  }
  userOutput = [];
  if (userInput % 1000 === 0) {
    userOutput.push('M');
    alert(userOutput);
  }
}


function toRoman(userInputStr){
  let userInput = parseInt(userInputStr);
  if (userInput >3900) {
    alert("No numbers over 3,900!!!")
  }
  userOutput = [];
  if (userInput % 1000 === 0) {
    userInput - 1000;
    userOutput.push('M');
  } 
  if (userInput % 500 === 0){
    userInput - 500;
    userOutput.push('D');
  }  
  if (userInput % 100 === 0) {
    userInput - 100;
    userOutput.push('C');
  }  
  if (userInput % 50 === 0) {
    userInput - 50;
    userOutput.push('L');
  }  
  if (userInput % 10 === 0) {
    userInput - 10;
    userOutput.push('X');
  }  
  if (userInput % 5 === 0) {
    userInput - 5;
    userOutput.push('V');
  } 
  if (userInput % 1 === 0) {
    userInput - 1;
    userOutput.push('I');
  }
    alert(userOutput);
}


function toRoman(userInputStr){
  let userInput = parseInt(userInputStr);
  if (userInput >3900) {
    alert("No numbers over 3,900!!!")
  }
  userOutput = [];
  if (userInput % 1000 === 0) {
    userInput - 1000;
    userOutput.push('M');
  } 
  if (userInput % 500 === 0){
    userInput - 500;
    userOutput.push('D');
  }  
  if (userInput % 100 === 0) {
    userInput - 100;
    userOutput.push('C');
  }  
  if (userInput % 50 === 0) {
    userInput - 50;
    userOutput.push('L');
  }  
  if (userInput % 10 === 0) {
    userInput - 10;
    userOutput.push('X');
  }  
  if (userInput % 5 === 0) {
    userInput - 5;
    userOutput.push('V');
  } 
  if (userInput % 1 === 0) {
    userInput - 1;
    userOutput.push('I');
  }
    
    alert(userOutput);
}


  
  
  
  







